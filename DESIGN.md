# CloudClub Attendance System Design System

## 1. Product character

CloudClub 출석 시스템은 행사 현장에서 휴대전화로 빠르게 쓰는 운영 도구다. 화면은 화려함보다 상태가 즉시 읽히고, 한 손으로 실수 없이 조작되며, 네트워크나 권한 문제를 한국어로 명확히 설명하는 것을 우선한다.

## 2. Visual foundation

- 기본 배경: 기존 관리자·학생 페이지의 짙은 남색/검정 계열 그라데이션을 유지한다.
- 표면: 반투명한 흰색(`rgba(255, 255, 255, 0.05)` 전후)의 글래스 카드와 얇은 테두리를 사용한다.
- 주요 강조색: 기존 파랑 `#60a5fa` 계열을 선택·진행·포커스 상태에 사용한다.
- 성공·경고·오류: 기존 의미 체계를 유지하고 색만으로 상태를 전달하지 않는다.
- 새 화면에서 임의의 새 색을 도입하지 않는다. 기존 CSS 변수나 가까운 기존 컴포넌트의 값을 재사용한다.

## 3. Typography

- 운영 화면의 기존 시스템 글꼴 스택과 크기 체계를 유지한다.
- 제목은 짧고 명확하게, 필드 라벨은 행동 또는 데이터의 의미를 직접 설명한다.
- 도움말과 상태문은 본문보다 한 단계 작게 쓰되 모바일에서도 읽을 수 있는 크기를 유지한다.
- 영문 API 명칭보다 사용자가 이해하는 한국어를 먼저 쓰고, 필요한 경우 괄호 안에 공식 명칭을 병기한다.

## 4. Spacing and layout

- 공통 간격 기준은 4px 단위다. 관리자 컴팩트 화면은 8px·12px·16px·20px을 주 간격으로 사용하고, 임의의 중간값을 새로 만들지 않는다.
- 관리자 콘텐츠 컨테이너는 데스크톱에서 최대 1760px까지 확장하되 좌우 안전 여백 20px을 유지한다. 모바일에서는 안전 여백을 12px으로 줄인다.
- 관리자 공통 카드는 데스크톱 20px, 모바일 16px의 내부 여백을 사용한다. 카드 간격은 데스크톱 16px, 모바일 12px을 사용한다. 정보량이 많은 표·차트는 숨기지 않고 카드 내부의 범위형 가로 스크롤을 유지한다.
- 관리자 폼의 라벨·입력·버튼 사이 기본 간격은 8px 또는 12px이며, 입력과 버튼은 정보 밀도를 높여도 최소 44px 조작 높이를 유지한다.
- 관리자 탭 콘텐츠는 모바일에서 한 열이다. 데스크톱에서는 내용의 독립성이 명확한 카드만 2열 또는 3열로 배치하고, 긴 표·대시보드·유고 매트릭스·변수 테이블은 전폭을 유지한다.
- 관리자 반응형 기준은 768px 이하 모바일 메뉴·1열, 1200px 이상 중간 데스크톱 다열, 1440px 이상 넓은 표면의 비대칭 다열, 1600px 이상 일정 관리 2열과 탭 한 줄 배치다.
- 카드 내부 여백, 필드 간 간격, 모달 폭은 기존 일정 관리 모달을 기준으로 한다.
- 폼은 모바일에서 한 열로 쌓이고, 넓은 화면에서만 정보 밀도를 해치지 않는 범위에서 병렬 배치한다.
- 새로운 위치 필드는 기존 날짜·시간 필드 다음에 배치해 `언제 → 어디서 → 추가 설명` 순서로 읽히게 한다.
- 터치 대상은 최소 44px 높이를 확보한다.
- 일정 설정 모달은 데스크톱에서 최대 960px 폭과 16px 열 간격을 사용한다. 왼쪽은 `날짜·시간·미리보기 → 선택 장소·반경·메모`, 오른쪽은 `장소 확인 토글·검색 → 큰 지도`로 나눠 지도 탐색 면적을 우선한다.
- 오른쪽 장소 열은 두 행을 세로로 이어 사용하고 지도는 남는 높이를 채운다. 선택 결과·반경·메모는 왼쪽 아래의 컴팩트한 세부 설정 패널로 이동해 지도 높이를 잠식하지 않는다.
- 900px 이하에서는 `날짜·시간 → 장소 검색·지도 → 선택 장소·반경·메모` 순서의 1열로 되돌리고, 시간 입력 두 개는 560px 이하에서만 세로로 쌓는다.
- 모달 외곽은 화면 안에 고정하고 본문만 필요할 때 스크롤하며, 열린 동안 배경 문서 스크롤을 잠그고 하단 저장·취소 행동은 항상 보이는 별도 행으로 유지한다.
- 밀도형 폼의 기본 간격은 8px, 섹션 간격은 12px 또는 16px을 사용하고, 메모 입력은 데스크톱 최소 72px·모바일 최소 84px을 유지한다.
- 모바일 하단 행동은 신규 회차의 2개 버튼과 기존 회차 수정의 3개 버튼을 모두 같은 너비의 한 줄 그리드로 수용한다.
- 긴 장소 주소는 공백 단위로 줄바꿈하되 마지막 번지나 짧은 토큰이 한 줄에 홀로 남지 않도록 균형 줄바꿈을 사용한다.

## 5. Components

- 관리자 앱 셸: 대형 히어로 대신 높이를 제한한 상단바를 사용한다. 브랜드 로고·`관리자` 시스템명은 왼쪽에 두고, 권한상 허용된 시스템 이동 탭은 같은 DOM을 재사용해 데스크톱 오른쪽에 한 줄로 이어 배치한다.
- 관리자 유틸리티 행: 로그인 사용자·역할·시즌과 참조 시트·새로고침 행동을 상단바 아래의 조밀한 2열 표면에 둔다. 모바일에서는 같은 정보 순서를 유지하며 1열로 쌓는다.
- 관리자 탭: `tablist`·`tab`·`tabpanel` 의미, `aria-selected`·`aria-controls`·`aria-labelledby`, roving tabindex를 함께 제공한다. 좌우 화살표·Home·End로 허용된 탭을 이동하고 활성 탭은 색과 `aria-selected`를 동시에 갱신한다.
- 관리자 모바일 메뉴: 오른쪽 상단 최소 44px 메뉴 버튼, 배경 오버레이, 오른쪽 슬라이드 패널로 구성한다. 데스크톱 탭과 별도 복제하지 않고 단일 탭 DOM을 뷰포트에 따라 재배치한다.
- 공통 인증 푸터: 공식 Cloud Club 페이지의 하단 구조를 기준으로 로고·소개문·원형 소셜 아이콘·Contact(이메일/Instagram)·저작권을 제공한다. 개인정보 처리 안내와 이용약관은 소셜 링크와 분리된 보조 법적 링크로 유지한다.
- Google Sheets 바로가기: 운영 데이터 원본으로 이동하는 주 행동은 기존 파란 버튼과 구분되는 성공 녹색 그라데이션을 사용하고, 같은 행의 정보 버튼에서 공식 계정 접근 제한과 직접 수정 금지 안내를 제공한다.
- 관리자 카드 그리드: QR은 2열, 출석현황 보조 카드와 운세 관리는 1200px부터 다열, 수료 판정·관리자 관리·시즌 진단은 1440px부터 비대칭 다열, 일정 관리는 1600px부터 2열을 사용한다.
- 카드: 기존 `.card`/모달 표면과 둥근 모서리(약 24px)를 재사용한다.
- 입력: 기존 폼 입력의 16px 안팎 모서리, 테두리, 파란 포커스 링을 재사용한다.
- 버튼: 주요 행동은 기존 파란 그라데이션 버튼, 보조·취소는 기존 보조 버튼 스타일을 쓴다.
- 토글: 체크박스와 설명을 한 덩어리로 제공하며 `장소 확인 필수`라는 결과를 문장으로 보여 준다.
- 장소 선택: Google 장소 자동완성 입력과 데스크톱에서 남는 세로 공간을 채우는 큰 조작형 지도를 오른쪽 탐색 패널에 묶는다. 선택된 장소 요약, Google Maps/제3자 attribution, 고정 반경, 선택적 메모는 왼쪽 세부 설정 패널에 조밀하게 배치한다. 지도는 장소 패널 안에서만 표시해 별도 화면 전환 없이 검색과 세부 위치 확인을 이어서 할 수 있게 한다.
- 신규 회차는 장소 기반 출석 확인을 기본으로 켠다. 기존 회차 수정은 저장된 `locationRequired` 값을 그대로 복원하고, 체크 해제 시 장소 입력 영역을 접어 위치 제한 없는 기존 출석 흐름으로 저장할 수 있게 한다.
- 검색 결과와 지도 장소 핀 선택은 동일한 Google Place ID 상태를 갱신하며, 선택 즉시 지도 중심·선택 표시·자동완성 검색 입력값을 동기화한다. Place ID가 없는 임의 좌표는 저장하지 않고, 지도에서는 Google 장소 핀을 선택하거나 검색 입력으로 주소를 찾도록 안내한다.
- 일정 설정 모달: 기존 입력 ID와 행동을 유지하는 반응형 2열 폼으로, 왼쪽 시간·세부 설정과 오른쪽 장소 탐색이 독립적인 정보 덩어리로 읽혀야 한다.
- 상태 메시지: 현재 위치 확인 중, 권한 거부, 정확도 부족, 반경 밖, 완료 상태를 텍스트와 아이콘/배지로 함께 표시한다.
- 학생 화면 탭: 데스크톱에서는 기존 글래스 탭 바의 시각 체계를 유지하고 `출석하기 → 출석 현황 → 행사 일정 → 수료 조건 확인` 순서로 제공한다. 모바일에서는 같은 네 항목을 오른쪽 상단 햄버거 버튼과 오른쪽 슬라이드 메뉴에 담는다.
- 모바일 메뉴: 메뉴 버튼·배경 오버레이·슬라이드 패널로 구성한다. 패널은 현재 탭을 텍스트와 강조 상태로 알리고, 열릴 때 첫 메뉴로 포커스를 이동하며 `Escape`·배경 누르기·메뉴 선택으로 닫힌다.
- 페이지 하단: 인증 게이트처럼 콘텐츠가 짧은 화면에서도 푸터가 viewport 하단에 닿도록 문서 루트를 세로 flex 컨테이너로 구성한다. 긴 관리자 콘텐츠에서는 자연스럽게 문서 마지막에 이어지며 fixed overlay로 본문을 가리지 않는다.
- 학생 지표 카드: 본인 출석률, 전체 평균, 평균 대비 차이, 전체 순위, 출석·지각·결석·유고 횟수를 동일한 정보 카드 그리드로 제공한다. 숫자는 크게, 단위와 비교 문장은 작고 명시적으로 표시한다.
- 수료 상태 카드: 서버가 계산한 수료 기준, 남은 수업과 최소 참여 횟수, 지각의 결석 환산값, 필수 회차 충족 여부, 현재 수료 가능 여부를 한 화면에서 위계적으로 보여 준다. 가능·불가 의미를 아이콘과 문장으로 함께 전달한다.
- 공통 전화번호 폼: 출석·현황·수료 탭 모두 `010`으로 시작하는 11자리 숫자를 같은 형식으로 입력받고, 마지막으로 입력한 번호를 세 폼에 동기화한다. 응답 데이터는 브라우저 영구 저장소에 보관하지 않는다.
- 학생 컴팩트 dashboard/detail: 정보 구조는 `출석하기 → 출석 현황 → 수료 조건 확인`의 3개 탭을 고정한다. `1200px` 초과에서는 넓은 dashboard 폭을 사용하고, `769px`부터 `1200px`까지는 같은 DOM 순서를 유지한 컴팩트 데스크톱 밀도로 줄이며, `768px` 이하는 모바일 메뉴와 1열 카드로 전환한다.
- 학생 출석 상세: 회차 목록은 접지 않고 결과에 항상 표시한다. 출석·지각·결석에서 공개 사유가 있는 행만 키보드와 포인터로 열 수 있는 트리거가 된다. 유고는 상태만 표시하고 사유를 전달하지 않는다. 상세 표면은 `role="dialog"`, `aria-modal="true"`, 제목·설명 연결, `Escape` 닫기, Tab 순환, 닫은 뒤 연결된 트리거 포커스 복원을 제공한다.
- 수료 정적 안내: `이 화면에는 스터디 출석이 반영되지 않습니다. 최종 수료 여부는 스터디 출석률에 따라 달라질 수 있습니다.`를 결과의 성공·실패와 무관하게 수료 결과 영역 앞에 항상 한 번 표시한다.

## 6. Interaction states

- 장소 확인이 꺼져 있으면 기존 출석 흐름과 일정 저장 흐름이 그대로 동작한다.
- 장소 확인을 켜면 유효한 Google Place ID 선택 전까지 일정 저장을 막고 이유를 표시한다.
- 학생 위치는 출석 버튼을 누른 뒤에만 요청한다. 요청 전 위치 수집 목적과 반경 조건을 알린다.
- 서버가 장소 정책 오류를 알려 주면 위치 권한을 요청하지 않고 출석 버튼을 비활성화한다.
- 로딩 중에는 중복 제출을 막고 진행 상태를 표시한다.
- 오류는 복구 행동을 함께 제시한다. 예: `위치 권한을 허용한 뒤 다시 시도해 주세요.`
- 학생 현황과 수료 정보는 서버 응답을 정본으로 사용한다. 아직 새 지표를 제공하지 않는 Apps Script와 연결된 경우 기존 출석 내역은 그대로 보여 주고, 추가 지표 영역에는 `Apps Script 업데이트 후 확인 가능`이라고 명시한다.
- 출석 성공 직후에는 같은 전화번호의 현황·수료 임시 캐시를 폐기해 다음 조회가 최신 서버 값을 사용하도록 한다.
- 모바일 메뉴가 열려 있는 동안 배경 문서 스크롤을 잠그고, 닫힌 뒤 메뉴를 열었던 버튼으로 포커스를 돌려보낸다.
- 학생 출석 상세 다이얼로그는 원본 Note를 HTML이나 data 속성에 넣지 않고, 서버가 공개한 `displayReason`만 `textContent`로 렌더링한다. 새 조회가 시작되면 이전 응답과 상세 캐시를 폐기하고, 이미 DOM에서 분리된 트리거에는 포커스를 강제로 되돌리지 않는다.

## 7. Student v6.3 current policy

- 현재 학생 기능 식별 계약은 2026.07.14-v6.3, studentAttendanceReasonV1=true, extractStudentDisplayReason=true입니다.
- 기존 status와 insights를 유지하고 선택적 안전 필드 details[].displayReason만 additive로 추가합니다.
- Pages-first 배포에서도 구버전 Apps Script의 기존 기능은 유지되고, 사유가 없거나 legacy인 행은 비대화형으로 남습니다.
- 공개 사유는 출석·지각·결석 Note의 선두 공개 영역에서만 읽으며, 유고는 사유를 공개하지 않습니다. 첫 번째 비어 있지 않은 비일치·내부 줄을 만나면 중단하므로 뒤의 일치 prefix는 공개하지 않습니다.
- Apps Script 배포는 운영자만 수행하며, 레포는 현재 외부 콘솔 상태를 단정하지 않습니다.
- 문제가 생기면 Pages는 직전 artifact로, Apps Script는 운영자가 직전 정상 배포 버전으로 롤백합니다.

## 8. Accessibility and responsive rules

- 관리자 상단바와 활성 패널은 sticky 영역에 가리지 않도록 `scroll-padding-top`과 `scroll-margin-top`을 제공한다.
- 관리자 모바일 메뉴가 열리면 배경 문서 스크롤을 잠그고 첫 허용 탭으로 포커스를 이동한다. `Escape`·배경 누르기·탭 선택으로 닫고 메뉴 버튼으로 포커스를 복원한다.
- 관리자 탭 버튼과 공통 행동 버튼은 `:focus-visible`에서 기존 파랑 포커스 링을 표시한다. 숨겨진 Super Admin 탭은 키보드 탭 순서에서도 제외한다.
- 관리자 화면은 375×667, 768×1024, 1280×900, 1920×1080에서 문서 자체의 가로 스크롤이 없어야 한다. 가로 스크롤은 탭/표처럼 명시된 내부 표면에만 허용한다.
- 모든 입력에는 시각적 라벨을 연결하고, 상태 변화는 `aria-live` 영역으로 알린다.
- 키보드 포커스가 보이도록 기존 파란 포커스 스타일을 유지한다.
- 장소 필수 여부와 오류를 색상에만 의존하지 않는다.
- 모달과 장소 자동완성은 키보드로 탐색·선택할 수 있어야 한다.
- 모바일 브라우저의 위치 권한 안내가 화면을 가리지 않도록 핵심 설명을 제출 버튼 가까이에 둔다.
- 학생·관리자 화면에서 공개 개인정보 처리 안내와 이용약관으로 이동할 수 있어야 한다.
- 모바일 기본 출석 상태는 375×667 화면에서 제목·시즌·출석 가능 시간·전화번호·제출 버튼·핵심 상태문이 한 화면에 들어오도록 헤더와 카드 여백을 압축한다. 결과·오류·위치 상세처럼 조건부 정보는 필요할 때 세로로 확장될 수 있다.
- 모바일 메뉴 버튼은 최소 44px 터치 영역과 `aria-expanded`, `aria-controls`를 제공하고, 슬라이드 패널은 `aria-hidden` 상태를 실제 열림 상태와 동기화한다.
- 모션 감소 설정에서는 슬라이드·페이드 전환 시간을 제거한다. 메뉴 전환은 `transform`과 `opacity`만 애니메이션하며 레이아웃 속성을 애니메이션하지 않는다.

## 9. Change discipline

- 현재 UI에는 일부 원시 색상과 매직 넘버가 남아 있다. 이번 기능은 그 값을 광범위하게 리팩터링하지 않고 기존 패턴을 보존한다.
- 관리자 컴팩트 리디자인은 `web/admin/*`의 앱 셸·공통 밀도·탭별 레이아웃 범위에 한정한다. 기존 입력 ID, 탭 ID, 권한 표시 속성, API 호출, 지연 로딩 계약은 바꾸지 않는다.
- 위치 출석 기능을 다시 수정할 때는 위치 출석 컴포넌트 범위를 유지하고, 관리자 앱 셸 전역 규칙을 우회하는 일회성 스타일을 추가하지 않는다.
- 장소 정책이 없는 과거 회차와 비-GPS 회차가 시각·동작상 회귀하지 않는 것을 우선 검증한다.
